<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Atividade javascript 1</title>
</head>
<body>
    <script>

// [JS-A01] Considere três notas (n1, n2, n3) e seus respectivos pesos (p1, p2, p3). Calcule a média ponderada das notas e atribua o resultado à variável "media". Após o cálculo, exiba a média ponderada no console.

// OBS: Para calcular a média ponderada multiplica os valores das notas pelos valores dos pesos, e divide pelas somas de todos os pesos. Lembre-se de declarar valores para os pesos e para as notas.
//  exemplo1

// let n1 = parseFloat(prompt("Digite a primeira nota(com peso 3):"));
// let n2 = parseFloat(prompt("Digite a segunda nota(com peso 4):"));
// let n3 = parseFloat(prompt("Digite a terceira nota(com peso 3):"));


// let p1 = 0.3;
// let p2 = 0.4;
// let p3 = 0.3;


// let media = (n1 * p1 + n2 * p2 + n3 * p3) / (p1 + p2 + p3);


// console.log("A média ponderada é:", media);


//EXEMPLO 2


let n1 = 7.5;
let n2 = 8.0;
let n3 = 6.5;

let p1 = 0.3;
let p2 = 0.4;
let p3 = 0.3;

let media = (n1 * p1 + n2 * p2 + n3 * p3) / (p1 + p2 + p3);

console.log("A média ponderada é:", media);


    </script>
</body>
</html>
